# {{PROJECT_NAME}}

This repository uses Project Template v1.1.

## Start here

```bash
project validate
project generate
project context project --budget small
```

Canonical intent lives in `docs/` and active objects under `knowledge/`. `.generated/` is disposable.
